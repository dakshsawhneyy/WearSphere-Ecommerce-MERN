apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wearsphere-helm.fullname" . }}-test-connection"
  labels:
    {{- include "wearsphere-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wearsphere-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
