apiVersion: v1
kind: Service
metadata:
  name: {{ include "wearsphere-helm.fullname" . }}
  labels:
    {{- include "wearsphere-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wearsphere-helm.selectorLabels" . | nindent 4 }}
